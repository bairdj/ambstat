# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
jobs:
  build_docker:
    machine: true
    steps:
      - run: docker build -t bairdj/ambstat:$CIRCLE_BRANCH .
workflows:
  build:
    jobs:
      - build_docker